# Local environments
.pixi/
.venv/
venv/

# Build and install outputs
build/
dist/
codec/build/

# Python caches
__pycache__/
*.py[cod]
*$py.class

# Logs and local scratch
*.log
.DS_Store
.env
.env.*

# Generated benchmark outputs.
# Historical files under results/ are still tracked, but new local runs should
# stay out of git unless they are deliberately promoted with `git add -f`.
results/*.json
results/*.csv
results/*.log

# Large local research samples / generated previews
data/
outputs/

# ML/generated artifacts
ml/training_data/
ml/structural_prob_data/
ml/hierarchy_data/
ml/residuals/
ml/checkpoints*/
*.pt
*.npz
